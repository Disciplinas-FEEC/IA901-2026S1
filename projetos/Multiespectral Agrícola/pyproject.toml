[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src"]

[project]
name = "multiespectral-agricola"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "ipykernel>=7.2.0",
    "matplotlib>=3.10.0",
    "numpy>=2.2.0",
    "pandas>=3.0.3",
    "pillow>=11.0.0",
    "python-dotenv>=1.2.2",
    "pytorch-lightning>=2.6.5",
    "pyyaml>=6.0.3",
    "seaborn>=0.13.2",
    "torch>=2.0.0",
]
